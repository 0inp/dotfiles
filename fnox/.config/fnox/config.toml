[providers.keychain]
type = "keychain"
service = "fnox"

[secrets]
GITHUB_PERSONAL_ACCESS_TOKEN= { provider = "keychain", value = "GITHUB_PERSONAL_ACCESS_TOKEN" }
LINEAR_API_KEY= { provider = "keychain", value = "LINEAR_API_KEY" }
