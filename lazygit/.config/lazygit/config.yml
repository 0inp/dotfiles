customCommands:
  - key: '<c-p>'
    context: 'localBranches'
    description: 'Publish PR via Claude (/pr-publish)'
    command: 'mux-new-window "pr-publish" "claude \"/pr-publish\""'
    subprocess: false
