customCommands:
  - key: '<c-p>'
    context: 'localBranches'
    description: 'Publish PR via Claude (/pr-publish)'
    command: 'tmux new-window -n "pr-publish" "claude \"/pr-publish\""'
    subprocess: false
