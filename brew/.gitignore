# Regenerated by every `brew bundle` run and specific to this machine's
# architecture and installed versions — 90 KB of pure churn in every diff.
# The Brewfile itself is the source of truth; the lockfile is a build artifact.
.config/brewfile/Brewfile.lock.json
